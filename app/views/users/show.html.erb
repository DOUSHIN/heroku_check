<h1><%= @user.name %>のページ</h1>
<p>メールアドレス:<%= @user.email %></p>
<%= image_tag(@user.image.url) %>

<% if logged_in? %>
  <%= link_to "Account", edit_user_path(@user.id) %>
  <%= link_to "likes", favorites_path %>
  <%= link_to "Post", new_picture_path %>
<% end %>
